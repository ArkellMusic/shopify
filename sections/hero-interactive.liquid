<section
  class="hero-interactive"
  id="hero-interactive"
  data-enter-url="/collections/all"
>

  <div class="hero-clouds" aria-hidden="true">
    <div class="cloud cloud-1"></div>
    <div class="cloud cloud-2"></div>
    <div class="cloud cloud-3"></div>
    <div class="cloud cloud-4"></div>
  </div>

  <div class="hero-overlay"></div>

  <div class="hero-interactive__content">

    <div
      class="hero-logo"
      id="hero-logo"
      role="img"
      aria-label="Logo AM"
      tabindex="0"
    >
      <img
        src="{{ 'logo-am.png' | asset_url }}"
        alt="AM Shop"
        class="hero-logo__image"
        draggable="false"
      >
    </div>

    <div class="hero-interactive__text">

      <h1 class="hero-title">
        {{ section.settings.title }}
      </h1>

      <p class="hero-subtitle">
        {{ section.settings.subtitle }}
      </p>

      <button
        class="hero-enter"
        id="hero-enter"
        type="button"
      >
        {{ section.settings.button_text }}
      </button>

    </div>

  </div>

  <div class="hero-scroll-hint">
    DESLIZA EL LOGO
  </div>

</section>

{% schema %}
{
  "name": "Hero AM",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "AM"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtítulo",
      "default": "Ropa y Música Electrónica"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Texto del botón",
      "default": "ENTRAR"
    }
  ],
  "presets": [
    {
      "name": "Hero AM"
    }
  ]
}
{% endschema %}
